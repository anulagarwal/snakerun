public enum UICanvas
{
    MainMenuCanvas,
    GameplayCanvas,
    GameOverCanvas
};

public enum SnakeCrawlDirection
{
    Up,
    Forward,
    Backward
};

public enum BeadsMovementStyle
{
    Normal,
    Slinky
};