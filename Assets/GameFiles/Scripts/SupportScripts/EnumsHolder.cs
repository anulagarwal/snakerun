public enum UICanvas
{
    MainMenuCanvas,
    GameplayCanvas,
    GameOverCanvas
};

public enum SnakeCrawlDirection
{
    Up,
    Forward
};

public enum MovementType
{
    Normal,
    Slinky
};

public enum SoundType
{
    Pop
};